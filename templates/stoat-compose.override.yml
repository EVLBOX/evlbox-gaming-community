# Override for Stoat self-hosted compose.
# Drops Stoat's Caddy (our stack Caddy handles all routing) and exposes
# key services on the shared evlbox network.
# Copied to /opt/evlbox/stoat/compose.override.yml by setup.sh

services:
  # Disable Stoat's built-in Caddy — our Caddy handles ports 80/443
  caddy:
    entrypoint: ["true"]
    restart: "no"
    ports: []

  # Expose services on the shared network so our Caddy can reach them
  web:
    networks:
      - default
      - evlbox

  api:
    networks:
      - default
      - evlbox

  autumn:
    networks:
      - default
      - evlbox

  january:
    networks:
      - default
      - evlbox

networks:
  evlbox:
    name: evlbox
    external: true
